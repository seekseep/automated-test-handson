# 01-setup

環境構築の確認用サンプル Web アプリ（Hono）。`docker compose up` で起動し、ブラウザで商品一覧が見えれば環境構築は成功。**テストはありません。**

```sh
cd sections/01-environment/01-setup
docker compose up
# → http://localhost:3000
```

詳細は [LECTURE.md](./LECTURE.md) / [docs/01_environment.md](../../../docs/01_environment.md)。
